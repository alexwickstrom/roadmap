from django.db import models
from django.contrib.auth.models import AbstractUser

# # Create your models here.
# class PaidUser(AbstractUser):
#     uname = models.CharField(max_length=200)
#     related_name = "paid_user"
